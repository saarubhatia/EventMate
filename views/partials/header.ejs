<!DOCTYPE html>
<html>

<head>
    <title>Event Registration</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
        integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <script src="https://code.jquery.com/jquery-3.5.1.js" integrity="sha256-QWo7LDvxbWT2tbbQ97B53yJnYU3WhH/C8ycbRAkjPDc=" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js"
        integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo"
        crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js"
        integrity="sha384-wfSDF2E50Y2D1uUdj0O3uMBJnjuUD4Ih7YwaYd1iqfktj0Uod8GCExl3Og8ifwB6"
        crossorigin="anonymous"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.0.0/animate.min.css" />
    <link href="https://fonts.googleapis.com/css2?family=Merriweather:ital,wght@1,300&display=swap" rel="stylesheet">
    <link href="/fontawesome-free-5.12.0-web/css/all.css" rel="stylesheet">
    <link rel="stylesheet" text="text/css" href="/stylesheets/main.css">
    <script src="/scripts/main.js"></script>
    <script src="/scripts/login.js"></script>
</head>

<body>
    <nav class="navbar navbar-dark navbar-expand-lg">
        <div class="container-fluid">
            <div class="navbar-header">
                <a class="navbar-brand" href="/events">EventMate</a>
            </div>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarNav">
                <% if(currentUser && currentUser.isAdmin){ %>
                <ul class="navbar-nav">
                    <li class="nav-item"><a class="nav-link <%= typeof page !== 'undefined' && page === 'options' ? 'active' : '' %>" href="/events/operations">Admin Options</a></li>
                </ul>
                <% } %>
                <ul class="nav navbar-nav ml-auto">
                    <% if(!currentUser){ %>
                    <li class="nav-item"><a href="/login" class="nav-link <%= typeof page !== 'undefined' && page === 'login' ? 'active' : '' %>"> Login </a></li>
                    <li class="nav-item"><a href="/register" class="nav-link <%= typeof page !== 'undefined' && page === 'register' ? 'active' : '' %>"> Sign Up </a></li>
                    <% } else { %>
                    <li class="nav-item"><a href="/user/<%=currentUser._id%>" class="nav-link">Hi, <%= currentUser.firstName %></a> </li>
                    <li class="nav-item"><a href="/logout" class="nav-link"> Logout </a></li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>
    <% if(error && error.length>0 || success && success.length>0 || info && info.length>0){ %>
    <div class="container">
        <% if(error && error.length>0){ %>
            <div class="alert alert-danger text-center" role="alert">
                <%= error %>
            </div>
        <% } %>
        <% if(success && success.length>0){ %>
            <div class="alert alert-success text-center" role="alert">
                <%= success %>
            </div>
        <% } %>
        <% if(info && info.length>0){ %>
            <div class="alert alert-info text-center" role="alert">
                <%= info %>
            </div>
        <% } %>
    </div>
    <% } %>